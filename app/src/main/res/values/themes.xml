<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HolyWaterTemple" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.HolyWaterTemple" parent="Base.Theme.HolyWaterTemple" />


    <style name="FullScreenTheme" parent="Theme.HolyWaterTemple">
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>



    <style name="DialogTheme" parent="Theme.HolyWaterTemple">
        <item name="android:background">#00000000</item>
        <!--全透明可以去掉dialog的角的颜色-->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowFrame">@null</item>
        <!--去掉title-->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <!--去掉多余的背景颜色-->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>